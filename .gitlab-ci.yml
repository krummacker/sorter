stages:
  - test

check:
  stage: test
  image: golang:latest
  script:
    - go version
    - go vet $(go list ./...)
    - go test -v $(go list ./...)
    - go run cmd/perfcheck/perfcheck.go

# test stage
include:
  - template: Jobs/SAST.gitlab-ci.yml
